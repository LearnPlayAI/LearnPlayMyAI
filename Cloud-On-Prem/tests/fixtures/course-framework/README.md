# Course Framework Fixtures

This directory contains deterministic fixtures for framework upload/extraction/generation tests.

## Structure
- `upload/`: source templates to convert into DOCX/PPTX/PDF test inputs.
- `extracted/`: expected extraction outputs for contract checks.
- `api/`: request payload fixtures.

## Sequencing Rule
Framework must be completed/finalized before lesson artifact generation checks.
