# UI Validation Gates

Run as applicable:
- `npm run -s check:ui-contrast`
- `npm run -s audit:theme-ui:strict`
- `npm run -s check:page-primitive-adoption`
- `npm run -s check:uikit-parity`
- `npm run -s check:uikit-coverage`
