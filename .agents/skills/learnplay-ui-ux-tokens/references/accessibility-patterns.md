# Accessibility Patterns

- Prefer semantic disabled foreground/background tokens over element opacity.
- Keep text readable in disabled or paused states.
- Use shared primitive fixes before page-level one-offs.
