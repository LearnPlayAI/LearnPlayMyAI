# UI Task Template (Design-First)

Use this template for all new UI pages and major UI changes.

## 1) Context
- Task:
- Target user:
- Business outcome:

## 2) Design Contract (Required Before Build)
- Primary user journey:
- Primary CTA:
- Required UI states:
  - loading:
  - empty:
  - error:
  - success:
- Breakpoints to validate:
  - `320`, `360`, `375`, `390`, `768`, `1024`, `1280`
- Theme Editor dependencies to preserve:
  - token groups:
  - brand assets:
  - org override behaviors:

## 3) UX Structure
- Route/page layout strategy:
- Information hierarchy:
- Non-modal interaction flow:
- Reusable/shared components to use:
- New shared component(s) to create (if any):

## 4) Data and Reliability
- Data sources:
- Mutation paths:
- Cache invalidation/revalidation strategy:
- Draft/persistence behavior:

## 5) Manual QA Plan
- Width checks:
- Edge-case content checks (long names, dense tables, empty datasets):
- Keyboard/touch/accessibility checks:
- Theme override checks:

## 6) Evidence and Handoff
- Screenshot paths (if captured):
- Residual risks:
- Recommended follow-up:

## 7) Shared-Component Roadmap Alignment
- Reused shared components:
- New shared components added:
- If no shared component changes were made, reason:
- Reference: `docs/ui/UI_SHARED_COMPONENTS_ROADMAP.md`
